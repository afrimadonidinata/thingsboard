{
  "name": "Java 17 + Maven + Node + Python",

  "image": "mcr.microsoft.com/devcontainers/java:17",

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y maven nodejs npm python3 python3-pip && java -version && mvn -version && node -v && npm -v && python3 --version",

  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-maven",
        "ms-python.python",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}
